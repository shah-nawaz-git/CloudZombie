# CloudZombie

CloudZombie is a self-hosted AWS cleanup planner that discovers resources deserving review through a strictly read-only boundary, tracks conditions over repeated scans, and estimates monthly list-price exposure without mutating AWS.
